<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".IncidentActivity" >

    <LinearLayout
        android:id="@+id/send_buttons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" >

        <Button
            android:id="@+id/take_photo_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="onSendButtonClick"
            android:text="@android:string/ok" />
    </LinearLayout>

    <ScrollView
        android:id="@+id/incident_form"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@id/send_buttons"
        android:layout_alignParentTop="true"
        android:src="@drawable/cichybohater_icon" >

        <fragment
            android:id="@+id/form_fragment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            class="pl.kodujdlapolski.cichy_bohater.gui.FormFragment"
            android:tag="form_fragment_tag" >
        </fragment>

        <!--
        <LinearLayout
            android:id="@+id/incident_form_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >
        </LinearLayout>
        -->
    </ScrollView>

</RelativeLayout>